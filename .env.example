# 이 파일을 복사해 .env 로 만들고 실제 값을 채우세요. (.env는 git에 올라가지 않음)
#   cp .env.example .env

# OpenAI API 키 (platform.openai.com > API Keys 에서 발급한 sk-... 전체 문자열)
OPENAI_API_KEY=sk-여기에-발급받은-키-붙여넣기

# 교사 모델 (기본 gpt-4o, 필요 시 변경)
TEACHER_MODEL=gpt-4o
